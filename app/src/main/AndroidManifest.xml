<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.banking_app">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_INTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACTION_OPEN_DOCUMENT" />
    <uses-permission android:name="android.permission.ACTION_GET_CONTENT" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Banking_app">

        <activity android:name=".activity.AccountDetailsActivity" />

        <activity android:name=".activity.AddPaymentActivity" android:theme="@style/Theme.Banking_app.popUpStyle">

        </activity>
        <activity android:name=".activity.EditProfileActivity" android:theme="@style/Theme.Banking_app.popUpStyle">
        </activity>

        <activity android:name=".activity.GenerateCardActivity" android:theme="@style/Theme.Banking_app.popUpStyle"/>

        <activity
            android:name=".Login"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivity"/>
        <activity android:name=".Signup"/>
    </application>

</manifest>